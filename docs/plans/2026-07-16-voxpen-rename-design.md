# VoxPen 重命名设计

## 目标

将项目品牌从 `VoxPen` 统一调整为 `VoxPen`，中文显示名为“声写”。

## 范围

- 重命名解决方案、项目目录、项目文件、程序集名和 C# 命名空间。
- 更新窗口标题、托盘提示、通知标题、CLI 帮助和 README。
- 保留 `CAPSWRITER_*` 环境变量及 CapsWriter 上游链接，维持兼容性和来源说明。
- 运行完整解决方案测试，确认重命名没有破坏引用。

## 不做的事

- 不迁移 Git 历史或远程仓库地址。
- 不修改模型目录结构、配置字段和用户数据路径。
