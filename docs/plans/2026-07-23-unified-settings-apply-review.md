# 统一设置保存与即时应用审查

**审查范围**: docs/plans/2026-07-23-unified-settings-apply-review-scope.md

## 结果

审查了设置写入、候选宿主预加载、旧宿主替换、ViewModel 重绑和用户文档。未发现 P0/P1 的逻辑错误或重复实现。

## 已确认行为

- 候选模型加载失败时，旧宿主不会释放，并恢复原来的快捷键和模型配置。
- 候选模型加载成功后，才释放旧宿主并启动新的全局监听器。
- 下载模型不再隐式保存或切换；唯一的“保存并应用”按钮负责提交并即时生效。
- Core 配置选择有独立 xUnit 覆盖；完整测试和 App 构建已执行。

## 未纳入本次修改

现有 `RecordingOverlayWindow.axaml` 的 Avalonia 过时 API 警告与本次改动无关，未处理。
